# 🤖 LLM-Powered Training Analysis

## ภาพรวม

ระบบนี้ใช้ **Google Gemini Vision Model** วิเคราะห์กราฟการเทรนแบบ Real-time และให้คำแนะนำว่าควรทำอะไรต่อ

## ✨ ฟีเจอร์

- ✅ **วิเคราะห์กราฟอัตโนมัติ** - สร้างกราฟและส่งให้ LLM วิเคราะห์ทุกๆ N iterations
- ✅ **คำแนะนำแบบ Real-time** - ได้รับคำแนะนำว่าควรเทรนต่อหรือหยุด
- ✅ **วิเคราะห์หลายมิติ** - ดู Policy Loss, Value Loss, Entropy, Explained Variance
- ✅ **บันทึกผลการวิเคราะห์** - เก็บ JSON และกราฟทุกครั้งที่วิเคราะห์
- ✅ **ภาษาไทย** - คำแนะนำและการวิเคราะห์เป็นภาษาไทยทั้งหมด

## 🚀 การติดตั้ง

### 1. ติดตั้ง Dependencies

```bash
pip install -r requirements.txt
```

### 2. ตั้งค่า Google AI API Key

#### Windows (PowerShell):

```powershell
$env:GOOGLE_API_KEY="your-api-key-here"
```

#### Windows (CMD):

```cmd
set GOOGLE_API_KEY=your-api-key-here
```

#### Linux/Mac:

```bash
export GOOGLE_API_KEY="your-api-key-here"
```

**หมายเหตุ:** รับ API Key ฟรีได้ที่ [Google AI Studio](https://makersuite.google.com/app/apikey)

## 📖 วิธีใช้งาน

### การเทรนพร้อม LLM Analysis

```python
# เทรนแบบปกติ (ไม่มี LLM Analysis)
python train_ppo.py

# หรือเรียกใช้ใน Python
from train_ppo import train_ppo

# เทรนพร้อม LLM Analysis (วิเคราะห์ทุกๆ 5 iterations)
train_ppo(use_llm_analysis=True, analysis_interval=5)

# เทรนโดยไม่ใช้ LLM Analysis
train_ppo(use_llm_analysis=False)

# เทรนและวิเคราะห์ทุกๆ 10 iterations
train_ppo(use_llm_analysis=True, analysis_interval=10)
```

### การใช้ LLM Analyzer แยกต่างหาก

```python
from llm_analyzer import LLMTrainingAnalyzer

# สร้าง analyzer
analyzer = LLMTrainingAnalyzer()

# บันทึก metrics จากการเทรน
for iteration in range(1, 26):
    metrics = {
        'loss': -0.02,
        'value_loss': 0.03,
        'entropy_loss': -1.0,
        'explained_variance': 0.5
    }
    analyzer.log_metrics(iteration, metrics)

# สร้างกราฟ
chart_path = analyzer.create_training_chart('my_chart.png')

# วิเคราะห์ด้วย LLM
analysis = analyzer.analyze_with_llm(
    chart_path,
    current_iteration=25,
    total_iterations=50
)

# แสดงผล
analyzer.print_analysis(analysis)

# บันทึกผล
analyzer.save_analysis(analysis, 'my_analysis.json')
```

## 📊 ตัวอย่างผลลัพธ์

### กราฟที่สร้างขึ้น

กราฟจะแสดง 4 metrics หลัก:

1. **Policy Loss** - ค่า loss ของ policy network
2. **Value Loss** - ค่า loss ของ value network
3. **Entropy Loss** - ระดับการสำรวจ (exploration)
4. **Explained Variance** - ความสามารถในการทำนาย

### ตัวอย่างการวิเคราะห์จาก LLM

```
================================================================================
🤖 LLM TRAINING ANALYSIS
================================================================================

🌟 สถานะโดยรวม: GOOD

📊 การวิเคราะห์:
  • policy_loss: Loss ลดลงอย่างต่อเนื่อง แสดงว่าโมเดลกำลังเรียนรู้ได้ดี
  • value_loss: มีความเสถียร ไม่มีการกระโดดขึ้นลงผิดปกติ
  • entropy: ลดลงค่อยๆ แสดงว่า policy กำลัง converge
  • variance: เพิ่มขึ้นเรื่อยๆ เข้าใกล้ 1.0 ซึ่งเป็นสัญญาณที่ดี

💡 คำแนะนำ:
  1. เทรนต่อไปได้ โมเดลยังมีแนวโน้มที่จะดีขึ้น
  2. ลองเพิ่ม total_timesteps เป็น 100,000 เพื่อผลลัพธ์ที่ดีขึ้น
  3. Monitor ว่า entropy ไม่ลดลงเร็วเกินไป (อาจทำให้ติด local optimum)

✅ ควรเทรนต่อ: โมเดลยังมีแนวโน้มที่จะดีขึ้นอีก
📈 ประมาณการ: ควรเทรนต่ออีก 15-20 iterations

================================================================================
```

## 📁 ไฟล์ที่สร้างขึ้น

เมื่อเทรนพร้อม LLM Analysis จะได้ไฟล์ต่อไปนี้:

```
train/
├── training_progress_iter_5.png      # กราฟที่ iteration 5
├── llm_analysis_iter_5.json          # ผลการวิเคราะห์ที่ iteration 5
├── training_progress_iter_10.png     # กราฟที่ iteration 10
├── llm_analysis_iter_10.json         # ผลการวิเคราะห์ที่ iteration 10
├── ...
├── training_progress_final.png       # กราฟสุดท้าย
└── llm_analysis_final.json           # ผลการวิเคราะห์สุดท้าย
```

## 🔧 การปรับแต่ง

### เปลี่ยนโมเดล LLM

```python
analyzer = LLMTrainingAnalyzer(model="gemini-1.5-pro")
```

### ปรับ Prompt

แก้ไขใน `llm_analyzer.py` ที่ฟังก์ชัน `analyze_with_llm()`:

```python
prompt = f"""
คุณเป็นผู้เชี่ยวชาญด้าน...
[ปรับแต่ง prompt ตามต้องการ]
"""
```

## ⚙️ Parameters

### `train_ppo()`

| Parameter           | Type | Default | คำอธิบาย                       |
| ------------------- | ---- | ------- | ------------------------------ |
| `use_llm_analysis`  | bool | True    | เปิด/ปิด LLM Analysis          |
| `analysis_interval` | int  | 5       | ทุกๆ กี่ iteration จะวิเคราะห์ |

### `LLMTrainingAnalyzer()`

| Parameter | Type | Default                | คำอธิบาย                                  |
| --------- | ---- | ---------------------- | ----------------------------------------- |
| `api_key` | str  | None                   | Google AI API Key (ถ้าไม่ใส่จะใช้จาก env) |
| `model`   | str  | "gemini-2.0-flash-exp" | ชื่อโมเดล LLM                             |

## 🎯 Best Practices

1. **ตั้งค่า API Key ให้ถูกต้อง** - ตรวจสอบว่ามี `GOOGLE_API_KEY` ใน environment
2. **เลือก analysis_interval ที่เหมาะสม** - ถ้าเทรนนาน ใช้ interval มากๆ (10-15)
3. **เก็บผลการวิเคราะห์** - ไฟล์ JSON มีประโยชน์สำหรับ compare ระหว่างการเทรนหลายครั้ง
4. **ใช้คำแนะนำจาก LLM อย่างชาญฉลาด** - LLM ให้คำแนะนำ แต่คุณต้องตัดสินใจเอง

## 🐛 Troubleshooting

### ❌ "GOOGLE_API_KEY not found"

**วิธีแก้:**

```bash
# ตั้งค่า API Key
export GOOGLE_API_KEY="your-api-key-here"

# หรือส่งตรงเข้าไปใน code
analyzer = LLMTrainingAnalyzer(api_key="your-api-key-here")
```

### ❌ "google-generativeai not installed"

**วิธีแก้:**

```bash
pip install google-generativeai
```

### ❌ LLM ตอบไม่เป็น JSON

**วิธีแก้:**

- ระบบจะพยายาม parse JSON อัตโนมัติ
- ถ้า parse ไม่ได้ จะส่ง raw text กลับมาใน `raw_response`
- ดูข้อมูลได้ที่ `analysis['raw_response']`

## 📚 เอกสารเพิ่มเติม

- [Google AI Python SDK](https://ai.google.dev/tutorials/python_quickstart)
- [Gemini API Documentation](https://ai.google.dev/docs)
- [Stable Baselines3 Documentation](https://stable-baselines3.readthedocs.io/)

## 💡 ตัวอย่างการใช้งานจริง

```python
# ตัวอย่าง: เทรนและให้ LLM ตัดสินใจว่าควรหยุดเมื่อไหร่
from train_ppo import train_ppo

# เทรนโดยให้ LLM วิเคราะห์ทุกๆ 3 iterations
# ถ้า LLM แนะนำให้หยุด จะถามผู้ใช้ก่อน
train_ppo(use_llm_analysis=True, analysis_interval=3)
```

## 🎉 สรุป

ระบบ LLM Analysis ช่วยให้คุณ:

- ✅ เข้าใจว่าการเทรนเป็นอย่างไร
- ✅ รู้ว่าควรทำอะไรต่อ
- ✅ ประหยัดเวลาในการ debug
- ✅ เรียนรู้ Reinforcement Learning ได้ดีขึ้น

Happy Training! 🚀
