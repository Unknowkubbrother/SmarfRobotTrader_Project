# 🚀 Quick Start Guide - LLM Training Analysis

## เริ่มต้นใช้งานภายใน 3 นาที!

### 1️⃣ ติดตั้ง Dependencies

```bash
cd train
pip install google-generativeai
```

### 2️⃣ ตั้งค่า API Key

รับ API Key ฟรีที่: https://makersuite.google.com/app/apikey

**Windows (PowerShell):**

```powershell
$env:GOOGLE_API_KEY="ใส่-api-key-ของคุณ-ที่นี่"
```

**Windows (CMD):**

```cmd
set GOOGLE_API_KEY=ใส่-api-key-ของคุณ-ที่นี่
```

### 3️⃣ ทดสอบระบบ

```bash
python test_llm_analyzer.py
```

คุณจะได้:

- 📊 กราฟการเทรน (`test_training_chart.png`)
- 🤖 การวิเคราะห์จาก LLM เป็นภาษาไทย
- 📄 ไฟล์ JSON (`test_llm_analysis.json`)

### 4️⃣ เทรนโมเดลจริง

```bash
python train_ppo.py
```

ระบบจะ:

- ✅ เทรนโมเดล PPO
- ✅ สร้างกราฟทุกๆ 5 iterations
- ✅ ให้ LLM วิเคราะห์และแนะนำ
- ✅ ถามคุณว่าควรหยุดหรือไม่ (ถ้า LLM แนะนำ)

## 🎯 ตัวอย่างผลลัพธ์

```
================================================================================
🤖 LLM TRAINING ANALYSIS
================================================================================

🌟 สถานะโดยรวม: GOOD

📊 การวิเคราะห์:
  • policy_loss: Loss ลดลงอย่างต่อเนื่อง แสดงว่าโมเดลกำลังเรียนรู้ได้ดี
  • value_loss: มีความเสถียร ไม่มีการกระโดดขึ้นลงผิดปกติ

💡 คำแนะนำ:
  1. เทรนต่อไปได้ โมเดลยังมีแนวโน้มที่จะดีขึ้น
  2. ลองเพิ่ม total_timesteps เป็น 100,000

✅ ควรเทรนต่อ: โมเดลยังมีแนวโน้มที่จะดีขึ้นอีก
================================================================================
```

## 🔧 การปรับแต่ง

### เปลี่ยนความถี่ในการวิเคราะห์

```python
# วิเคราะห์ทุกๆ 10 iterations แทน 5
train_ppo(use_llm_analysis=True, analysis_interval=10)
```

### ปิด LLM Analysis

```python
# เทรนแบบปกติ (ไม่มี LLM)
train_ppo(use_llm_analysis=False)
```

## 📚 เอกสารเพิ่มเติม

อ่านเพิ่มเติมที่: `LLM_ANALYSIS_README.md`

## 💡 Tips

1. **ใช้ API Key ของคุณเอง** - ฟรี! ไม่มีค่าใช้จ่าย (มี quota)
2. **เก็บไฟล์ JSON** - มีประโยชน์สำหรับเปรียบเทียบ
3. **ดูกราฟ** - เข้าใจการเทรนได้ดีขึ้น
4. **ฟัง LLM แต่ใช้วิจารณญาณ** - LLM ให้คำแนะนำ แต่คุณตัดสินใจ

---

Happy Training! 🎉
